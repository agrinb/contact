<div class="container-fluid">
  <h1>Listing notes</h1>
    <%= render partial: "notes_list", object: @notes %>

  <br>


  <% if @mycontact %>
    <%= link_to 'New Note', new_user_mycontact_note_path(@user, @mycontact, @note) %>
  <% end %>
</div>
